{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement.id }}</td>
            </tr>
            <tr>
                <th>PaiementStatus</th>
                <td>{{ paiement.paiementStatus }}</td>
            </tr>
            <tr>
                <th>Method</th>
                <td>{{ paiement.method }}</td>
            </tr>
            <tr>
                <th>PaiementApi</th>
                <td>{{ paiement.paiementApi }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paiement_index') }}">back to list</a>

    <a href="{{ path('paiement_edit', {'id': paiement.id}) }}">edit</a>

    {{ include('paiement/_delete_form.html.twig') }}
{% endblock %}
